# MusicPlayer
Player de Musica

## Bibliotecas necessárias

Para o funcionamento do programa, instale as seguintes bibliotecas Python:

```bash
pip install pillow 
pip install pygame
pip install tkinter
```
